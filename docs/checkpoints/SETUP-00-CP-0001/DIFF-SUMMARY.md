# Diff Summary

The empty baseline became a SETUP-00-only repository containing:

- canonical agent, schema, template, and policy layers under `.iacode/`;
- semantically aligned Codex and Claude Code adapters;
- architecture, roadmap, Gate, quality, handoff, checkpoint, model-use, and provenance documentation;
- five initial ADRs;
- four reusable prompts;
- standard-library checkpoint creation, finalization, validation, and redaction scripts;
- isolated automated tests for valid lifecycle and required adversarial corruption cases;
- the initial Engineering Ledger checkpoint.

No Gate 0 runtime, model gateway, agent runtime, sandbox, or application feature was implemented.

