# Next

## Required next action

Await explicit authorization before beginning `GATE 0 — FOUNDATION`. If authenticated Claude Code validation is desired first, run the clean-clone sequence in `HANDOFF.md` with `C:/Users/cesar/.local/bin/claude.exe`, record the actual model/tool metadata returned by Claude, and create a new checkpoint for that evidence. Do not modify or retag either sealed SETUP-00 checkpoint.
