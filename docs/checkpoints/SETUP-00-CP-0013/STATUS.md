# Status

MILESTONE_INDEPENDENT_AUDIT_PASS

The fresh-session independent audit of the sealed `SETUP-00-CP-0012`. `CP11-F-001` is `CLOSED` and
no new finding was raised. `M0` is `PASSED`, carried by this audit checkpoint about its subject, and
`SETUP-00` is closed.

The mechanism is `FRESH_SESSION_INDEPENDENT_AUDIT`: independent of the implementing run and of its
session, not of the tool, the provider or the model. `crossToolValidation` is `NOT_AVAILABLE`, and
`MILESTONE_EXTERNAL_PASS` is refused for this mechanism.

`GATE 0 - FOUNDATION` is authorised by the milestone and is not started.
