# Lesson Candidates from M0 Audit

These are audit-owned OBSERVED candidates. They were not promoted into .iacode/memory/lessons.jsonl; Claude Code must assess provenance, category, severity, applicability and guardrail evidence in the corrective checkpoint.

1. Apply one shared promotion invariant to every positive terminal state.
2. Treat the mandatory Green Keeper gate set as closed, complete and non-empty.
3. Anchor completeness to an independently derived required-ID set and cross-check every aggregate.
4. Resolve structured controls/evidence and recompute derived artifacts instead of trusting syntactic claims.
5. Bind dirty-tree command inputs immutably and make sealing chronology monotonic.

The detailed observations and proposed automated guardrails are in LESSON-CANDIDATES.json.

