# The build context is the repository root, so this file is only used when a build is invoked with
# apps/api as the context. infra/compose/docker-compose.yml builds from the root and relies on the
# root .dockerignore instead.
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
