# Provenance Policy

Every potentially reusable artifact must identify source type, provider, model, ownership, license, permitted storage, RAG use, training, distillation, evidence, and notes.

Unknown permission is denial. `trainingAllowed` and `distillationAllowed` default to `false`. Raw external-model output is never marked trainable solely because it is stored in this repository.

